{
  "dependencies": {
    "syncano-client": "^0.2.1",
    "syncano-server": "beta"
  },
  "scripts": {
    "e2e": "cross-env NODE_ENV=test mocha 'tests/*.js' --reporter spec --require babel-register --timeout 4000"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babel-register": "^6.22.0",
    "chai": "^3.5.0",
    "chai-as-promised": "^6.0.0",
    "cross-env": "^3.1.4",
    "mocha": "^3.2.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
